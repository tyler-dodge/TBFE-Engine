#ifndef StringConv_L
#define StringConv_L
#include <sstream>
#include <fstream>
using namespace std;
template <class T> inline std::string toString (const T&);
std::string loadString(ifstream *, char);
extern std::string nextSet(string *,char);
#endif
