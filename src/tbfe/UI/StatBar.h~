#ifndef StatBar_L
#define StatBar_L
#include "Element.h"
class StatBar:public Element_Base
{
 private:
  SDL_Surface * StatBarSurface;
  SDL_Surface * StatBarBack;
  SDL_Rect Bar;
  SDL_Rect BarDimensions;
 public:
  float Percentage;
  void RenderElement(SDL_Surface *, Position);
  void Reload();
  StatBar(int,int,string);
  ~StatBar();
};
#endif
