#ifndef Element_Base_L
#define Element_Base_L
#include "../misc/Structs.h"
#include "../misc/SdlFunctions.h"
#include "../Base.h"
class Element_Base
{
 private:
 public:
  SDL_Surface * Background;
  Position CurrentPosition;
  Position Dimensions;
  std::string Name;
  std::string Special;
  bool Visible;
  virtual void RenderElement(SDL_Surface *, Position)=0;
  virtual void Reload()=0;
  Element_Base(int,int);
};
#endif
